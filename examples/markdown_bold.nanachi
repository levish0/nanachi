// Stateful parsing: bold markers with re-entrance prevention.

let flag inside_bold

text = { !("**") ANY }

inline = { bold | text }

bold = {
    guard !inside_bold
    with inside_bold {
        "**" inline+ "**"
    }
}
